# Motor de Notificaciones IA — dependencias de producción
# Instalar con: pip install -r requirements.txt

# ── Pipeline de datos ──────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0           # Lectura del Excel de Power BI
psycopg2-binary>=2.9.0    # Conector PostgreSQL
requests>=2.31.0          # Llamadas HTTP al servicio IA desde el pipeline

# ── Machine Learning ───────────────────────────────────────────
scikit-learn>=1.4.0
joblib>=1.3.0
matplotlib>=3.7.0
seaborn>=0.13.0

# ── API del modelo ─────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
httpx>=0.27.0              # Tests del endpoint desde el notebook

# ── Notebook ───────────────────────────────────────────────────
nest-asyncio>=1.6.0
ipykernel>=6.29.0
jupyter>=1.0.0
